# this should deal with profiles, locking/releasing/creating/etc.. profiles
# all the config for a profile should be stored in a 'Profile' object which can be accessed from this ProfileManager. It should replace [::config::g/setKey *] calls...


snit::type ProfileManager {

}